{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pip",
  "description": "Capability for the performance PiP window",
  "windows": ["performance-pip"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "core:window:default"
  ]
}
